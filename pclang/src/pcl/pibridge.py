"""pibridge — PiBridge 占位（M2 交付，DESIGN §8）。

pi 适配（``pi --mode rpc`` 子进程 + JSONL RPC + 上下文 = pi session）与
embed 传输（§8.5）属 M2/M3 里程碑；当前经 ``bridge.make_bridge`` 拒绝并提示。
"""

from __future__ import annotations
