#!/usr/bin/env pcl
${:# 场景：库复用——.pcl 间 import（与 Python 互操作同机制）}
${import greetlib}
Calling the shared library function:
${_ = greetlib.greet("PCL")}
(the line above is rendered by a template function from greetlib.pcl)
